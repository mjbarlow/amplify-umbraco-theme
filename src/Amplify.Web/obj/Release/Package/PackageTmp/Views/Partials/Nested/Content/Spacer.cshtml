@inherits Amplify.Core.ViewPages.AmplifyViewPage<Spacer>
@using Umbraco.Web.PublishedModels

<section class="section amplify-spacer @RazorHelper.BackgroundClass(Model.BgColor.Label)">
    <div class="container">
        <div class="columns is-centered is-mobile">
            <div class="column is-6-tablet is-9-mobile">
                @if (!Model.HideSpacerLine)
                {
                    <hr class="@RazorHelper.BackgroundClass(Model.SpacerColor.Label)" />
                }
            </div>
        </div>
    </div>
</section>
